create table entries(
	roll_no varchar(10),
	status varchar(5),
	at_time varchar(10),
	on_date varchar(12),
	visitor_no varchar(10),
	phone_no int,
	reason_for_visiting varchar(256),
	admission_no varchar(10),
	first_name varchar(20)
);
